import { Canvas, Meta } from '@storybook/blocks';
 
import * as CalloutStories from './Callout.stories';
 
<Meta of={CalloutStories} />

# Callout

A callout visually highlights a section of content within an interface.

<Canvas of={CalloutStories.BasicExample} />

## Inline callout

<Canvas of={CalloutStories.InlineCallout} />

## Accessibility

The `Callout` component has a default role of `note` and is labeled by the `CalloutHeading`. If CalloutHeading is not included, please add the `aria-label` prop to the `Callout` component. Alternatively, the `dialog` role can be used for the Callout component.

<Canvas of={CalloutStories.Accessibility} />