import { Meta, Canvas } from '@storybook/blocks';

import * as SkeletonStories from './Skeleton.stories';

<Meta of={SkeletonStories} />

# Skeleton

A **skeleton** is a placeholder UI that is displayed while data is loading. Use the **Skeleton** component to create a skeleton UI.

<SkeletonStories.BasicExample />