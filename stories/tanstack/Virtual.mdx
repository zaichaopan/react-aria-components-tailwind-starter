import { Canvas, Meta } from '@storybook/blocks';
 
import * as TanstackStories from './Virtual.stories';
 
<Meta of={TanstackStories} />

# Tanstack/virtualizer

Integrate <a href="https://tanstack.com/virtual/latest" target="_blank">**Tanstack/Virtualizer**</a> with `Autocomplete` and `ListBox`.
 
<Canvas of={TanstackStories.BasicExample} />