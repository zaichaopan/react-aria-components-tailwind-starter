import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import * as CardStories from './Card.stories';

<Meta of={CardStories} />

# Card

An **Card** is a container that displays content and actions on a single topic. It is a flexible and extensible content container.

<Canvas of={CardStories.BasicExample} />